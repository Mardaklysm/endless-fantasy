# Endless Validation Matrix

Pick the smallest validation that covers the change. `npm run build` is the default code validation, and `git diff --check` should run before commit.

## docs-only

Use for: `agent_knowledge/`, README, comments, prompts, or non-runtime docs.

Validation commands:

```powershell
git diff --check
```

What not to run by default: `npm run build`, `npm test`, Vite, browser smoke.

## code-static

Use for: TypeScript-only edits, data tables, settings models, non-visual logic with no worldgen semantic changes.

Validation commands:

```powershell
npm run build
git diff --check
```

What not to run by default: `npm test`, Vite, browser smoke.

## ui-menu-settings

Use for: menus, settings UI, HUD/panel text, input-visible UI changes.

Validation commands:

```powershell
npm run build
git diff --check
```

What not to run by default: browser smoke unless visual layout or runtime interaction cannot be checked by static review.

## save-load

Use for: save payloads, load normalization, localStorage settings, migration/default fields.

Validation commands:

```powershell
npm run build
git diff --check
```

What not to run by default: `npm test` unless worldgen/dungeon generation validation changed.

## harbor-travel-runtime

Use for: harbor destination menus, boat travel state, sailing speed, arrival/despawn behavior.

Validation commands:

```powershell
npm run build
git diff --check
```

What not to run by default: `npm test` unless `src/world/semantic` or world generation validation changed. Browser smoke only when visual arrival/movement behavior changed or the user asks.

## worldgen-semantic

Use for: semantic generation, terrain, roads, rivers, harbors generated by worldgen, validation rules, worldgen lab behavior.

Validation commands:

```powershell
npm test
npm run build
git diff --check
```

What not to run by default: browser smoke unless visual/runtime behavior also changed.

## movement-collision

Use for: player movement, collision, location entry, dungeon movement.

Validation commands:

```powershell
npm run build
git diff --check
```

What not to run by default: `npm test` unless semantic/worldgen collision sources changed. Browser smoke only when runtime movement needs confirmation.

## battle

Use for: battle flow, action resolution, enemy intent, rewards, battle UI.

Validation commands:

```powershell
npm run build
git diff --check
```

What not to run by default: `npm test`, Vite/browser smoke unless visual battle behavior changed or a manual encounter check is requested.

## assets-art

Use for: asset manifests, imports, texture keys, runtime asset mapping.

Validation commands:

```powershell
npm run build
git diff --check
```

What not to run by default: `npm test` unless worldgen asset prerequisites or semantic visual selection changed. Avoid opening large image folders unless the asset task requires it.

## browser-smoke

Use for: visual/runtime behavior that build cannot verify, or when the user explicitly asks.

Validation commands:

```powershell
npm run dev
```

Then inspect the changed surface at `http://127.0.0.1:5173`.

What not to run by default: for docs, settings/data-only changes, or static code changes.

## full-regression

Use for: broad cross-system work, release checks, or explicitly requested regression passes.

Validation commands:

```powershell
npm test
npm run build
git diff --check
```

Add browser/manual checks only with a concrete reason.

What not to run by default: full regression should be rare and explicitly justified.
